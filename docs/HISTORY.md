# 진행 기록 (결정·발견 로그)

설계 문서(`DESIGN.md`)는 "지금의 결론"을, 이 문서는 **어떻게 그 결론에 왔는지**를 남긴다.
새 결정이나 실기기 발견이 생기면 아래에 이어서 적는다.

---

## 1. 목표와 원칙 (2026-09-23, 출발점)

- 키보드·마우스를 Windows / Android / Linux 사이에서 공유한다. **1순위는 Windows ↔ Android.**
- 호스트가 리시버를 등록하고, 조건(단축키 또는 화면 경계)이 충족되면 입력을 해당 기기로 보낸다.
- **서버 없음, 같은 네트워크(가까이 있는 기기)에서만.**
- 한 기기가 호스트·리시버로 모두 등록될 수 있고, 둘 다면 시작할 때 고른다. 백그라운드에서 동작한다.

## 2. 설계 결정 (`DESIGN.md` §11)

| ID | 결정 | 근거 |
|---|---|---|
| D1 | Rust 코어 + Kotlin(Android) + Tauri(Desktop) | 3개 OS 공용 코어, 저수준 입력 API 접근성 |
| D2 | 전환은 단축키 먼저, 화면 경계는 나중 | Android 호스트·Wayland는 전역 커서 위치를 알기 어려움 |
| D3 | Android 리시버: Shizuku 특권 방식 기본 + 접근성 대체 | 일반 앱은 다른 앱에 입력을 넣을 권한이 없음 |
| D4 | 한 기기는 동시에 한 역할만 활성 | 입력 루프·포커스 충돌 방지 |
| D5 | 리시버는 한 번에 호스트 하나 | 단순화 |
| D6 | "같은 네트워크" = 사설 대역 + 같은 서브넷 | 서버리스 원칙 |
| D7 | `Ctrl+Alt+→/←`, 비상 복귀 `Ctrl+Alt+Shift+Esc` | 충돌 적은 조합 |
| D8 | Android **호스트는 블루투스(BLE) HID로 전달**, Android **리시버는 Shizuku UHID** | 아래 §4~5의 실기기 결과 |

## 3. P0 기술 검증 — 1차 (Windows → Android)

| 발견 | 결과·조치 | 커밋 |
|---|---|---|
| 주입한 `MotionEvent` 는 **시스템 마우스 포인터를 움직이지 않는다** (포인터는 실제 장치에서만 그려짐) | 리시버 기본을 **UHID 가상 장치**로 변경 (scrcpy UHID 모드와 같은 방식) | `39e7f44` |
| 로컬 컴파일 검증에 쓴 Robolectric `android-all` 은 **숨김 API까지 포함**해서, 공개 SDK에 없는 `MotionEvent.setActionButton` 사용을 못 잡음 | 리플렉션으로 호출, 이후 CI(실제 SDK) 빌드를 기준으로 삼음 | `93ec203` |
| Windows LL 훅 캡처·차단 + UHID 리시버 | **실기기 정상 동작** ("잘 돼") | — |

## 4. Android 호스트 — 방향 결정 과정

1. **"호스트를 블루투스 기기로 인식시킬 수 있나?"**
   - Android는 앱이 블루투스 키보드·마우스 역할을 할 수 있음. Windows는 불가(동글 필요), Linux는 가능.
   - 장점: 대상 기기에 앱 설치 불필요, 암호화는 블루투스 페어링이 담당.
2. **"Shizuku(개발자 옵션) 없이 쓰고 싶다" → "개발자 옵션 작업 자체가 번거롭다"**
   - 개발자 옵션 없이 다른 앱에 진짜 입력을 넣는 방법은 Android에 없음 → 무권한 경로는 접근성 서비스 또는 하드웨어 동글뿐.
3. **결정 (D8)**: Android 호스트 = 블루투스, Android 리시버 = Shizuku.
   호스트 입력원은 **폰에 연결한 실제 키보드·마우스**.

## 5. P0-5 — Android 호스트 실기기 결과

| 증상 | 원인 | 조치 | 커밋 |
|---|---|---|---|
| Shizuku 없으면 호스트가 멈춤 | 캡처가 항상 Shizuku 경로였음 (블루투스 쪽은 권한 불필요) | Shizuku 없으면 **접근성 서비스로 캡처** (키 필터링) | `ad4e5c5` |
| 여전히 "Shizuku 때문에 멈춤"처럼 보임 | 접근성 서비스가 꺼져 있었는데 오류 문구가 Shizuku 실패처럼 읽힘. 또 Shizuku 헬퍼 버전이 고정이라 **APK 업데이트 후에도 옛 헬퍼 재사용** | 시작 전 안내 창(제한된 설정 허용 포함), 빌드마다 헬퍼 버전 변경 | `0e05678` |
| `kmc demo` 연결 거부 | 리시버 대신 호스트를 켰거나, 리시버가 UHID(Shizuku 필요)로 시작 실패 | 거부/시간초과별 안내 문구, 리시버 실패 토스트 | `6db5224` |
| **호스트를 켜면 폰의 블루투스 키보드·마우스가 끊김** | **클래식 HID 장치 역할이 폰의 HID 호스트와 같은 L2CAP 채널(PSM 0x11/0x13)을 점유** | **BLE HID(HOGP)로 전환**, 클래식은 비교용 옵션으로만 | `fbf2f2f` |
| (진단) 끊김 시점 추적 | — | 블루투스 연결/해제·본딩 이벤트 로그 | `f9ee54d` |
| Windows 장치 추가 목록에 폰이 안 뜸 | Windows 11 기본 검색은 "기타 장치"를 숨김. Android 앱은 광고에 기기 종류(Appearance)를 넣을 수 없음 | "Bluetooth 장치 검색 = 고급" 안내 (P0 가이드) | `a6860c5` |
| **동작은 하지만 버벅임, 지연 요동** | 알림을 1개씩만 보내고, 밀리면 오래된 것부터 버림 (키 입력 누락 가능) | 이동 병합 큐(키·클릭은 절대 병합·폐기 안 함, 클릭을 넘어 병합 안 함), 동시 3개, 짧은 연결 간격 요청, 연결 후 광고 저전력 | `9794c59` |
| **Windows 조작 중 폰 포인터도 같이 움직임** | 접근성 모션 가로채기는 이벤트만 가로채고 포인터는 시스템이 먼저 움직임 | 대상 조작 중 투명 오버레이 + **포인터 캡처** (폰 포인터 숨김·고정) | `7776709` |
| 최종 | **Shizuku 없이 BLE 호스트 정상, 버벅임 없음** | P0 결과 기록 | `7157fca` |

## 6. KeymaHub (Android 제품, 처음 이름 KemaHub) 착수

- 결정: 첫 유료 제품은 **Android 블루투스 허브만**. 이름 KemaHub, Play 스토어 비공개(closed) 배포부터.
- 결정: **단축키만** (`Ctrl+Alt+1~9` = 대상 번호, `Ctrl+Alt+0` = 이 폰). 화면 경계는 뒤로.
- 결정: **BLE(HOGP)만**. 클래식 HID·Shizuku·리시버 모드는 제품에서 제외 (P0 앱에만 남김).
- 구현: 새 모듈 `android/keymahub` (당시 `android/kemahub`, 패키지 `com.kemahub` → 지금 `app.keymahub`, Compose UI). P0에서 검증한 BLE HID·전송 큐·접근성 캡처·포인터 캡처를 옮기고,
  번호 슬롯 라우터(`SlotRouter`)·대상 번호표(`SlotTable`)·포그라운드 서비스·설정 안내(접근성 사전 고지 포함)를 새로 작성.
- 단위 테스트: 슬롯 단축키·전환 시 키 해제 순서·번호 교환·전송 큐.
- 변경 (v0.3): 단축키 배치를 **`1` = 이 폰, `2~9, 0` = 리시버**로 바꾸고 **사용자 지정** 가능하게 함 (조합이 정확히 일치해야 전환, 녹화 중에는 전환 멈춤).
  **프로필**(기기 배정 + 단축키)을 여러 개 두고 전환. **영어·한국어·일본어·중국어·스페인어·프랑스어**.
  첫 실행 시 호스트 모드 권한(블루투스·알림)을 바로 요청하고 접근성 고지로 이어짐.
- 실기기: **Windows로 넘기면 폰 터치가 전혀 안 됨** → 포인터 캡처 오버레이가 화면 전체를 덮는 터치 가능 창이었음.
  1×1, 터치 불가·터치 비모달 창으로 변경. 폰을 터치하면 포커스가 앱으로 가서 캡처가 풀리므로, 마우스를 다시 움직이면 캡처를 되찾음 (Android 14+).

- 변경 (v0.4): 화면 개편 — 메인은 호스팅 스위치 + 프로필 목록, ⋮ 메뉴에 기기 관리·설정.
  **페어링 모드**(켜져 있을 때만 새 기기 허용), 기기 연결 해제(재연결 거부)·별칭·마지막 사용, 언어·테마 설정.
  **프로필 활성화 조건**(시간·장소·둘 다) + 목록 순서 = 우선순위, 수동 고정.

- 변경: 조건에 맞는 프로필이 없으면 **마지막으로 활성화한 프로필** 사용(조건 없는 프로필은 직접 활성화할 때만). 순서는 ☰ 드래그, 행 끝에 활성화·편집 버튼.
- 실기기: Windows 조작 중 폰 가상 키보드가 안 뜸 → 물리 키보드 연결 시 Android가 숨기는 기본 동작. 대상 조작 중에만 접근성 서비스로 "하드 키보드 무시" 표시 모드(Android 10+).

- 변경: 단축키를 프로필·슬롯별 녹화 → **앱 전체 보조키 조합 + 고정 숫자**(기본 Shift+Alt, 1 = 이 폰, 2~9·0 = 기기). Windows에서 Alt+Shift가 언어 전환이라, 전환 시 대상에 F24를 끼워 넣음.

- 변경: 프로필의 **장소 조건을 "연결된 기기" 조건으로 교체**. 지도 선택 요청을 검토하다가, 가까이 오면 자동 재연결되는 BLE 기기가 장소보다 정확하고 위치 권한·Play 위치 신고·지도 키가 필요 없어서 결정.

- 변경: 프로필 **활성화 방식 3가지**(설정): 완전 자동(연결된 배정 기기 수 최다, 기본) / 규칙 기반 자동 / 끄기(수동).

- 변경: **시간 조건 제거**. 규칙 기반 자동은 "고른 기기 연결 시"만. 시간 경계에서 작업 중 전환이 튀고, 기기 조건으로 대부분 충분.

- 변경: 앱 이름 **KemaHub → KeymaHub** (화면·알림·접근성 설명·릴리스 파일명).

- 변경: **앱 아이콘**(키보드·마우스 허브 그림, 적응형·테마 아이콘) 적용. 코드 패키지 `com.kemahub` → **`com.keymahub`**, 앱 ID `com.kemahub.app` → **`com.keymahub.app`**, 모듈 `android/kemahub` → `android/keymahub`, 접근성 서비스 `KeymaAccessibilityService`.
  앱 ID가 바뀌어 기존 설치본과는 별개 앱(설정·페어링 다시). 릴리스 secrets 이름은 `KEYMAHUB_*`(예전 `KEMAHUB_*`도 읽음).
- 변경: 저장소·프로젝트 이름 **keymanc → keymahub** (Rust 크레이트 `keymahub-proto`·`keymahub-keymap`, PoC 앱 패키지 `dev.keymahub.poc`, Gradle 루트 `keymahub-android`, BLE 제조사 이름 `KeymaHub`).
  아이콘 여백은 원본 그림과 같은 비율(그림 폭 = 보이는 아이콘 폭의 약 82%).
- 실기기: 호스팅을 껐다 켜면 **태블릿이 자동으로 다시 붙지 않음** → BLE 키보드는 광고만 하고 연결은 대상이 거는데, 일부 호스트(특히 Android 태블릿)는 키보드 쪽이 끊은 뒤로는 다시 걸지 않음.
  호스팅을 켤 때(그리고 "연결 허용" 때) **폰이 페어링된 기기에 먼저 자동 연결**(백그라운드 대기, 차단한 기기 제외). "연결 해제"는 폰 쪽 연결도 닫아 링크가 실제로 끊기게 함.
- 변경: **P0 테스트 콘솔 앱(`android/app`) 삭제**. KeymaHub가 쓰는 BLE·캡처 코드는 모두 옮겨졌고, 나머지(리시버 UHID·Inject, Shizuku, IME, evdev 프로브)는 제품에서 제외한 방식. 코드는 git 기록에 남김(커밋 `71410e3` 시점에 있음). CI의 P0 APK 빌드·첨부도 제거.
- 변경: 앱 ID(패키지명) `com.keymahub.app` → **`app.keymahub`**. 코드 패키지는 `com.keymahub` 그대로. 앱 ID가 바뀌어 기존 설치본과는 별개 앱(설정·페어링 다시).
- 변경: 앱을 종료하면(최근 앱에서 닫기) **진단 로그 초기화**. 접근성 서비스 때문에 프로세스가 살아 있어 로그가 계속 쌓였음.
- 변경: 코드 패키지도 `com.keymahub` → **`app.keymahub`** (앱 ID와 같게).
- 실기기: 앱 설치 후 **첫 페어링이 실패**(연결 0.1초 만에 끊김), 페어링 모드를 껐다 켜면 됨 → 연결 즉시 광고를 껐다 켜던 것이 막 만들어진(암호화·페어링 전) 링크를 끊음. 광고 재시작을 대상이 준비된 뒤로 미룸. 연결·끊김 로그에 사유 코드 표시.
- 실기기 로그: 폰 자신의 블루투스 키보드·마우스(MX Keys·Master) 연결도 대상으로 잡혀 연결될 때마다 광고를 재시작했고, 호스팅을 끄면 그 연결까지 끊음 → KeymaHub 기기만 연결 시 대상으로, 그 외 기기는 HID 서비스를 쓸 때만 대상(미페어링은 페어링 모드에서만).
  페어링된 기기 중 아직 안 돌아온 것이 있으면 광고를 중간 속도(약 250ms)로 해 PC·태블릿이 빨리 다시 찾게 함. Windows는 PC가 광고를 하지 않아 폰이 먼저 연결할 수 없고, 광고를 보고 PC가 붙어야 함.
- 실기기: 위 변경 후 태블릿이 호스팅 재시작 뒤 다시 붙지 않음 → 이 폰은 링크가 생기면 광고가 멈추고, 태블릿의 HID는 폰 광고를 보고 스스로 연결해야 동작. 페어링된 기기와 링크가 생기면 바로 광고 재시작, 페어링 중인 새 기기만 준비 후로 미룸.
- 실기기: 재시작 후 태블릿이 **연결은 되지만 준비(ready)가 안 됨** → HOGP에서는 장치가 페어링된 호스트별 알림 설정(CCCD)을 연결이 끊겨도 기억해야 하고, 호스트는 재연결 때 다시 켜지 않음. 폰은 매번 지우고 CCCD 쓰기를 기다렸음. 기기별로 저장했다가 재연결 시 복원("ready (restored)").
- 실기기: 호스팅을 끄고 1초 만에 켜면 이전 세션의 연결이 새 세션에 잡혔다가 곧 끊기고, 재연결 예약이 없어 그대로 끝남 → 새 세션의 서비스가 준비되기 전 연결은 무시.
- 실기기: 재연결 후 폰은 "ready (restored)"인데 **태블릿에는 "연결됨"이 안 뜸** → 폰이 먼저 연 링크는 태블릿 HID가 자기 키보드 연결로 쓰지 않고, 오히려 태블릿이 스스로 연결하는 것을 막음. 폰이 먼저 연결하는 기능(#3) 제거, 광고만 하고 호스트가 연결하게 둠. "연결" 버튼은 폰 쪽 링크를 닫고 빠른 광고로 전환.
- 실기기: 위 변경 후 Windows가 호스팅 재시작 뒤 다시 붙은 것처럼 보였으나, 바로 켜서 Windows가 끊김을 알아채기 전이었음(실제로는 태블릿과 같은 상태). Android 태블릿은 여전히 태블릿에서 직접 연결해야 함 → HID Information 플래그에 RemoteWake 추가(0x02 → 0x03, 실제 키보드와 같게)해 호스트가 재연결을 기다리게 하는 실험.
- 실기기: 태블릿에서 직접 연결하면 폰은 "ready (restored)"인데 **태블릿은 "연결 중"에서 멈춤** → 복원 직후(태블릿이 아직 암호화·HID 연결 중) 연결 간격 요청과 광고 재시작을 해서 절차가 꼬임. 링크가 자리 잡도록 3초 뒤로 미룸.
  태블릿은 호스팅 재시작 후 스스로 연결을 시도하지 않음(거절 기록 없음) — 호스트가 폰 광고를 알아보지 못하는 쪽.
- 실기기: **페어링 모드일 때만** 태블릿이 제대로 재연결되고, 아닐 때는 앱에서만 연결됨 → 링크는 태블릿으로 알아보지만, 태블릿의 첫 GATT 요청은 아직 임시 주소로 와서 "페어링 안 됨, 페어링 모드 꺼짐"으로 거절했음(기기 목록에 주소만 있는 항목이 생긴 원인도 같음). 요청 단계에서는 거절하지 않고 응답만 하며 대상으로 등록하지 않음. 페어링 모드가 꺼져 있을 때 HID 서비스를 쓰던 기기가 새로 페어링을 시작하면 그때 끊음.
- 실기기: 폰에서 호스팅을 끈 뒤에는 태블릿에서 직접 연결해도 "연결 중"에서 멈추고, 태블릿 블루투스를 재시작해야 풀림(첫 페어링·태블릿 재시작 후에는 정상) → 끊기 요청 직후 GATT 서버를 닫아, 아직 연결된 태블릿이 HID 서비스가 사라지는 것(Service Changed)을 봄. 링크가 실제로 끊긴 뒤(최대 3초) 서버를 닫음. 시작·종료는 한 번에 하나씩.
- 변경: **호스팅을 켜면 페어링 모드를 1분 자동으로 켬**(등록된 기기가 모두 돌아오면 바로 끔). 실기기에서 태블릿은 페어링 모드일 때만 확실히 재연결됐고, 원인(호스트 쪽 주소 인식 등)을 앱에서 끝까지 확정하기 어려워 확인된 동작을 이용. 그 1분 동안은 새 기기도 페어링 가능.
- 진단: 태블릿 재연결이 간헐적(된 경우엔 연결 직후 태블릿이 구독을 다시 보내고, 안 된 경우엔 아무 요청도 없음) → 각 링크의 처음 GATT 요청 40개(읽기·쓰기, 특성·인스턴스, 값), 광고 속도·이름 포함 여부, 앱 버전·기기를 로그에 남김. 진단 로그 화면에 메모 입력(시도·결과를 그 시각에 기록).
- 조사·결정: **BLE 구조 단순화**. 일반 BLE 키보드는 광고만 하고 호스트가 저장해 둔 정보(서비스 구성, 마지막으로 본 주소)로 다시 붙음. 실기기에서 "앱이 연결됨인 동안 태블릿 블루투스를 껐다 켜면 붙고, 앱이 끊김을 처리(광고 재시작)한 뒤엔 안 붙음"을 확인 → 광고를 새로 시작할 때마다 바뀌는 임시 주소와, 호스팅마다 GATT 서버를 닫았다 다시 만드는 것이 원인.
  GATT 서버·서비스와 광고 세트(AdvertisingSet)를 한 번 만들어 블루투스가 켜져 있는 동안 유지. 호스팅 켜기/끄기 = 광고 켜기/끄기 + 대상 연결 끊기, 페어링 모드 = 스캔 응답의 이름만 교체. 광고 속도 단계·호스팅 시작 페어링 창(#15)·이전 세션 연결 처리·종료 시 링크 대기(#17, 서버를 닫지 않으므로 불필요) 제거.
- 실기기(#18 후): 호스팅 켜기 후 태블릿 자동 재연결 성공(19:37:29 → 6초 뒤 ready restored), 태블릿 블루투스 재시작도 1~4초 안에 재연결. 남은 문제: **호스팅을 꺼도 태블릿 링크가 실제로 끊기지 않음**(앱은 연결 종료, 태블릿은 연결됨) → Android 앱의 끊기는 자기 사용분만 놓고, 다른 앱·시스템(삼성 기기 간 연동 등)이 쓰는 링크는 남음. 다시 켜도 태블릿은 이미 연결돼 있어 새로 붙지 않아 어긋남.
  호스팅을 켤 때 이미 연결된 등록 기기를 바로 이어받음(구독 복원), 끈 뒤 남은 링크는 로그에 기록.
- 정정: 사용자 확인 결과 **호스팅만 켜서 태블릿이 붙은 적은 없음**. 앱의 "연결됨·ready (restored)"는 거짓이었음 — 요청 추적을 보면 그 링크들은 배터리(2A19)만 읽고 구독(삼성·Android 시스템이 여는 링크)했고, 진짜 키보드 연결은 매번 2A4A·2A4B(키 배치표)·2908을 읽고 입력 보고(2A4D) 구독을 새로 씀. CCCD 복원(#10)과 등록 기기 링크를 대상으로 잡는 처리(#19 이어받기 포함) 제거: **HID 서비스를 쓰는 기기만 대상, 키보드 입력을 구독해야 준비됨.** 근본 문제는 "폰이 끊은 뒤 태블릿 HID가 스스로 다시 붙지 않음"으로 정리.
- 실기기: 호스팅을 끄면 "still linked after stopping: dean's tab" — **폰이 끊어도 링크가 남음**(시스템·다른 앱이 유지). 태블릿은 키보드 연결 유지, 앱은 끊김으로 어긋나고 다시 켜도 태블릿은 새로 구독하지 않음.
  결정: **호스팅 끄기 = 일시정지**(광고·입력만 중지, 연결 유지). 다시 켜면 연결된 기기로 즉시 입력. 링크가 실제로 끊기는 건 거리·호스트 조작·블루투스 꺼짐뿐이고, 그때 재연결 여부는 거리 테스트로 확인.
- 재작성: **BLE 연결 관리**(`BleHid`) 다시 작성, 연결 판단은 Android 없이 테스트되는 `HostTable`로 분리. 방향: 호스팅 끄기 = 연결 끊기, 켠 상태 전환 = 연결 유지, 켜기·거리 복귀 = 자동 재연결.
  - 알림 설정(CCCD)을 **페어링별로 저장**(앱 재시작에도 유지). HOGP에서 CCCD는 본딩의 일부라 호스트는 재연결 때 다시 쓰지 않음 — **Windows가 재연결에 한 번도 성공하지 못한 원인**으로 추정(링크는 붙어도 폰이 구독을 기다림). 페어링된 호스트가 전에 구독했으면 링크가 붙는 즉시 준비됨.
  - #10의 복원이 거짓 "연결됨"을 낸 건 호스팅마다 GATT 서버를 닫아 태블릿 HID가 닫혀 있었기 때문(#18 이전). 지금은 서버를 유지하므로 링크가 살아 있으면 호스트 HID도 살아 있음.
  - 호스팅 끄기: 광고 끔 + 대상 링크를 놓음(폰의 키보드·마우스는 제외). 시스템이 유지하는 링크는 남고 로그에 기록.
  - 호스팅 켜기: 광고 켬 + **이미 붙어 있는 알려진 호스트의 링크를 이어받음**(`GattServer.connect`는 링크가 있을 때만; 폰이 새 링크를 여는 일은 없음). "태블릿은 연결됨인데 폰은 반응 없음"의 원인(폰이 상태를 지워 구독을 기다림) 해소.
  - 호스팅 중에는 광고를 계속 켜 둠(연결 직후는 3초 뒤 재개) — 거리로 끊긴 호스트가 돌아올 수 있게.
  - 윈도우에서 처음 페어링하면 "기타 디바이스"로 보이다가 HID 드라이버 설치 후 키보드로 옮겨짐(정상). 광고의 외형(Appearance) 값은 Android 앱 API로 넣을 수 없음.
- 실기기(#22 후, Windows PC): **호스팅 재시작 후 Windows 자동 재연결 성공** — 켠 지 2초 만에 PC가 광고를 보고 붙고 "ready (restored)", PC는 CCCD를 다시 쓰지 않음(저장한 구독으로 바로 입력). 링크가 남아 있던 경우엔 켜자마자 이어받아 즉시 입력.
  남은 문제: **호스팅을 꺼도 PC는 연결됨**("still linked after stopping"). 삼성 기기가 아닌 PC에서도 남음 — 우리 앱의 서버 연결·빠른 링크를 모두 놓아도 폰의 다른 앱·시스템(Windows와 연결/Phone Link 등으로 추정)이 링크를 유지. 링크가 언제 끊기는지 10초 간격으로 기록하도록 진단 추가.
- 변경: 진단 로그 **이메일 보내기 → 클립보드 복사**(앱 버전·기기 정보 포함).
- 변경: 프로필 화면의 **기기(단축키) 행을 누르면 그 기기로 제어 전환**(호스팅 중·사용 중인 프로필일 때, 단축키와 같은 경로라 연결 안 된 기기는 같은 안내). 빈 번호를 누르면 기기 고르기. 기기 고르기·빼기 메뉴는 행 끝 **⋯ 버튼**으로.
- 변경: 프로필 화면의 활성화 조건 설명을 **하나로 합침**: "프로필 활성화 방식이 '규칙 기반 자동'일 때, 고른 기기 중 하나라도 연결되면 이 프로필이 자동으로 활성화됩니다." (방식이 다를 때 뜨던 별도 안내 카드 제거)
- 변경: 메인 화면 프로필 목록의 **편집 버튼 제거, 행을 누르면 프로필 편집 화면**으로.
- 실기기(#23 후, Windows): 호스팅을 끄면 여전히 "still linked after stopping". 분석: Android 스택은 폰이 주변기기인 링크를 **그 링크를 잡은(hold) 앱이 모두 놓을 때만** 끊는데, 호스트가 먼저 연 링크는 아무 앱도 잡고 있지 않아 `cancelConnection`만으로는 아무 일도 일어나지 않음.
  변경: 호스팅 끄기·연결 해제·거절 때 **서버가 먼저 링크를 잡고(`connect`) 0.5초 뒤 놓음**(`cancelConnection`) → 마지막으로 놓는 앱이 되어 스택이 링크를 끊게 함(알려진 우회법). 잡는 동안 오는 연결 콜백은 호스트 재연결로 보지 않음. 그래도 남으면 다른 앱이 잡고 있는 것 — 진단 로그로 확인.
- 변경: 프로필에서 **지금 제어 중인 기기를 다른 번호로 옮기면 제어도 그 번호로 따라감**(제어 중이 아닌 기기는 그대로).
- 변경: 프로필 화면의 **기기(번호) 목록을 ☰ 끌기로 순서 변경**(프로필 목록과 같은 방식). 1번(이 폰)은 고정, 2~9·0번 사이에서 기기(빈 자리 포함)가 옮겨지고 번호·단축키는 그대로. 제어 중인 기기가 옮겨지면 제어도 따라감.
- 실기기(레노보 Legion Y700 5세대 태블릿): 재연결 실패 원인 추적. 태블릿 dumpsys: 폰을 `6d:3a(페어링 때 임시 주소) => 80:b0(공개 주소) [DUAL]`로 기억. **태블릿 HID가 켜질 때 링크가 이미 있으면 열리고(`HIDH Opened`, 폰에는 요청 없이 캐시로 열림 — "ready (restored)"가 진짜일 수 있음), 태블릿이 새로 연결하면 암호화까지 되고도 HID가 안 열림**("연결 중" 고정). 같은 태블릿의 MX Keys 등은 주소가 하나라 정상. 태블릿 블루투스 플래그 `hogp_reconnection` 꺼짐. 호스팅을 꺼도 태블릿이 링크를 유지(10분 이상) — 태블릿이 연 링크라 폰 쪽 끊기가 안 먹힘.
  실험 추가: ① 설정 > 실험 > **고정 주소로 광고**(공개 주소 광고, 시스템 API라 거부될 수 있음, 켠 뒤 태블릿에서 다시 페어링) ② 기기 관리 **"연결" 버튼이 링크가 없으면 폰이 먼저 태블릿에 링크를 엶**(태블릿 광고 시 연결).

- **갤럭시 루틴에서 프로필 활성화** (2026-09-26): 삼성 모드 및 루틴은 서드파티 앱 동작을 공개 SDK 없이 **앱 바로가기(App Shortcuts)**로만 받는다(루틴 동작 → 앱 → 바로가기). 그래서 프로필마다 동적 바로가기를 만들고, 바로가기는 화면 없는 `ActivateProfileActivity`를 열어 `activate(id)` 후 토스트만 띄우고 닫힌다. 같은 액티비티를 명시적 인텐트(action·profile id/이름 extra)로도 열어 Tasker 등에서 쓸 수 있게 했다. 바로가기 개수는 런처 한도(보통 4~5개)까지라 목록 위쪽 프로필만 노출된다.

- **설명 다듬기** (2026-09-26): 설명을 (?) 팝업으로 옮겼다가(#34) 화면에 다시 두고, **긴 문장만 짧게** 줄임(프로필 목록·활성화 조건·단축키·페어링 모드·보조키·화면 키보드·고정 주소 실험). 활성화 방식 설명만 기존처럼 (?) 팝업. Windows 안내는 "장치 추가 창에서 '모든 장치 표시'"(실기기에서 확인, 기본 검색은 종류를 모르는 기기를 숨기는 것으로 보임).

## 7. 운영 규칙

- 커밋 작성자: `dean <pqzmggg@gmail.com>`, 커밋 메시지에 공동 작성자·세션 줄을 넣지 않는다 (기존 이력도 재작성함).
- `main`에는 직접 푸시하지 않고 **항상 PR로 머지**한다.
- CI(GitHub Actions)는 **PR에서만** 돈다(PR 생성·PR 브랜치에 푸시할 때, 필요하면 Actions 탭에서 수동 실행). `main` 머지 후에는 다시 돌지 않는다.
  Rust 테스트·clippy, Windows exe, Android APK를 빌드하고 산출물을 올린다. Android 빌드 검증은 CI 기준. 릴리스 빌드는 release 워크플로.

## 8. 남은 확인 항목 (2026-09-23 기준)

- BLE 대상 2대 이상 동시 연결·즉시 전환 (`Ctrl+Alt+→` 반복)
- BLE로 한/영 키가 Windows에서 전환되는지
- PC 재부팅·절전 복귀 후 자동 재연결
- Windows → Windows: Windows 리시버(`SendInput`) 미구현 (블루투스 경로는 Windows 호스트에서 불가)
