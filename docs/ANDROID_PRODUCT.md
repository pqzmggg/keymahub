# keymanc Android 제품 기획 (v0.1 초안)

> P0에서 검증된 기술을 실제로 매일 쓸 수 있는 앱으로 만들기 위한 기획.
> **§12의 결정 항목**을 정하면 v0.2로 확정하고 개발(M1)에 들어간다.

---

## 1. 한 줄 정의

**폰·태블릿에 연결한 키보드·마우스로 옆의 PC와 태블릿까지 조작하는 앱.**
대상 기기에는 아무것도 설치하지 않고 블루투스로 붙이며, 서버도 계정도 없다.
(선택) PC의 키보드·마우스로 폰을 조작하는 기능도 제공한다.

## 2. 사용자와 핵심 시나리오

| # | 시나리오 | 역할 | P0 상태 |
|---|---|---|---|
| S1 | 태블릿 + 블루투스 키보드·마우스로 작업하다가, 옆의 Windows 노트북도 **같은 키보드·마우스**로 조작 | 호스트 | ✅ 검증 |
| S2 | 노트북·데스크톱·iPad를 번갈아 조작 (단축키 한 번에 즉시 전환) | 호스트 | 구현됨, 미검증 |
| S3 | PC 앞에서 일하다가 폰 메시지에 **PC 키보드·마우스로** 답장 | 리시버 | ✅ 검증 (Shizuku + PC 쪽 프로그램 필요) |

**하지 않는 것**: 화면 미러링, 파일 전송, 인터넷 너머 원격 조작, 게임용 초저지연.

## 3. 제품 원칙

1. **설치 후 5분 안에 첫 전환.** 필요한 설정만, 필요한 순간에 요청한다.
2. **입력이 지금 어디로 가는지 항상 보인다.** (알림, 전환 순간의 화면 표시)
3. **언제나 빠져나올 수 있다.** 비상 단축키, 대상 끊김 시 자동 복귀, 앱이 죽으면 모든 가로채기 해제.
4. **입력 내용을 저장하거나 외부로 보내지 않는다.** 대상 기기로만 전달.

## 4. 기술 기반 (P0 결론, `HISTORY.md`)

| 역할 | 입력 받기 | 입력 보내기 | 필요한 설정 |
|---|---|---|---|
| 호스트 | 접근성 서비스(키보드) + 포인터 캡처(마우스) | **BLE HID** (대상은 블루투스 페어링만) | 블루투스 권한, 접근성 켜기 |
| 호스트 (고급) | Shizuku로 장치 직접 잡기 | BLE HID | + Shizuku |
| 리시버 | Wi-Fi로 PC에서 수신 | Shizuku UHID 가상 장치 | Shizuku + PC 프로그램 |

- 클래식 블루투스 HID는 폰의 블루투스 키보드·마우스를 끊기 때문에 제품에서는 뺀다 (진단용으로만 남길지는 §12).
- 대상 기기 조건: BLE 키보드를 지원하는 OS (Windows 10/11, macOS, iPadOS/iOS, Android, 최신 Linux).

## 5. 기능 범위

| 기능 | v1.0 (MVP) | 이후 |
|---|:-:|:-:|
| 호스트: BLE 키보드·마우스로 대상 조작 | ● | |
| 호스트: 대상 여러 개 등록, 단축키로 순환·즉시 전환 | ● | |
| 호스트: 대상별 직접 전환 단축키 (`Ctrl+Alt+1..9`) | ● | |
| 전환 순간 화면 표시(HUD)·상시 알림(현재 대상, 빠른 전환·일시정지 버튼) | ● | |
| 대상 관리: 이름 변경, 전환 순서, 삭제 | ● | |
| 단축키 변경 | | ● |
| 한/영 키 처리 확인·대상별 보정 | ● (확인) | ● (보정) |
| 미디어 키(볼륨·재생), 소비자 키 | | ● |
| 마우스 속도·스크롤 방향 대상별 설정 | | ● |
| 부팅 시 자동 시작, 마지막 역할 기억 | ● | |
| 권한 체크리스트·진단 로그 공유 | ● | |
| 리시버 모드 (PC → 폰, Shizuku) | §12 결정 | |
| Windows 앱 (호스트·리시버), Wi-Fi 페어링·암호화 | | ● (제품군) |
| 클립보드 텍스트 공유 | | ● (Wi-Fi 경로 필요) |
| 화면을 터치패드로 쓰는 리모컨 모드 (권한 불필요) | | ○ 검토 |

## 6. 사용자 흐름

### 6.1 첫 실행 (호스트)
```
환영 → "이 폰의 키보드·마우스로 다른 기기 조작" 선택
 → ① 알림 허용 → ② 근처 기기(블루투스) 허용
 → ③ 접근성 켜기 (설정 화면으로 이동, 돌아오면 자동 확인)
      └ APK 설치본이라 막히면: "앱 정보 → ⋮ → 제한된 설정 허용" 안내 화면
 → ④ 첫 대상 추가 (6.2)
 → ⑤ 연습: "Ctrl+Alt+→ 를 눌러 보세요" → 전환 성공 표시
```
각 단계는 **이미 되어 있으면 건너뛴다.** 중간에 나가도 다음 실행 때 이어서 한다.

### 6.2 대상 추가
- 앱 화면: 대상 OS 선택(Windows / Mac / iPad / Android) → OS별 그림 안내.
  - Windows: 설정 → 블루투스 및 장치 → 장치 추가 → 블루투스 → **이 폰 이름**.
    목록에 없으면 "Bluetooth 장치 검색 = 고급" 안내 (Windows 11 기본 검색이 숨김).
  - 페어링하는 동안 폰의 블루투스 설정 화면을 열지 않도록 안내 (클래식 "휴대폰"으로 잘못 붙는 것 방지).
- 대상이 연결되면 자동으로 목록에 추가되고 이름을 붙이게 한다.

### 6.3 일상 사용
- 앱을 닫아도 상시 알림이 떠 있다: "이 폰 조작 중 / ▶ 데스크톱 조작 중", 버튼 [다음 대상] [일시정지].
- `Ctrl+Alt+→`: 대상으로 (이미 대상이면 다음 대상), `Ctrl+Alt+←`: 폰으로, `Ctrl+Alt+Shift+Esc`: 비상 복귀.
- 전환할 때 폰 화면에 잠깐 표시: "→ 데스크톱", "← 이 폰".
- 대상이 꺼지거나 멀어지면 자동으로 폰으로 돌아오고 알림으로 알린다.

### 6.4 역할이 둘 다 있을 때 (설계 R8)
- 시작 시 역할 선택 + "기억하기". 알림에서 역할 전환.

### 6.5 문제 상황
| 상황 | 앱의 반응 |
|---|---|
| 접근성 서비스가 꺼짐 (시스템이 끄는 경우 포함) | 알림으로 알리고 한 번에 설정으로 이동 |
| 포인터 캡처가 거부됨 | 대체 방식으로 계속 동작 + "폰 포인터도 움직일 수 있음" 안내 |
| 이 기기가 BLE 주변기기 모드를 지원 안 함 | 첫 실행에서 바로 알림 (호환성 표 링크) |
| 대상이 재연결 안 됨 | 대상 화면에 "다시 페어링" 안내 |

## 7. 화면 구성

1. **홈**: 상태 카드(켜짐/꺼짐, 현재 조작 대상), 연결된 대상 목록(활성 표시), 시작/중지.
2. **대상**: 목록·이름·순서·삭제, [대상 추가] 가이드.
3. **단축키**: 전환·다음·복귀·비상 (v1.0은 보기만, v1.1 변경).
4. **설정**: 부팅 시 시작, 한/영 키, (고급) Shizuku 캡처 사용.
5. **권한·진단**: 필요한 권한 체크리스트, 로그 보기·공유, 호환성 정보.
6. (리시버 포함 시) **PC에서 이 폰 조작**: Shizuku 상태, PC 연결.

UI는 Jetpack Compose + Material 3. P0 콘솔 화면은 "개발자 메뉴"로 숨겨 남긴다.

## 8. 백그라운드·배터리

- 호스트가 켜져 있는 동안 Foreground Service(`connectedDevice`) + 상시 알림.
- BLE 광고: 대상이 없을 땐 빠르게, 대상이 준비되면 저전력 (P0에서 구현).
- 배터리 최적화 예외는 첫 실행에서 요청하지 않고, 끊김이 감지될 때 안내 (심사·사용자 거부감 고려).
- 목표: 호스트 켜 둔 채 대기 시 배터리 소모 **시간당 2% 이하** (측정 필요).

## 9. 배포·정책

| 항목 | 내용 |
|---|---|
| **접근성 API (가장 큰 리스크)** | Google Play는 접근성 API를 장애 보조 목적 외로 쓰면 선언과 심사가 필요하고, 거절 사례가 많다. keymanc는 "물리 키보드 입력 가로채기"라 심사 통과가 불확실하다. |
| APK 직접 배포 | GitHub Releases 등. 대신 Android 13+에서 "제한된 설정 허용" 단계가 추가된다. |
| Foreground Service 유형 | Play Console에 `connectedDevice` 사용 사유 신고 필요. |
| 개인정보 | 키 입력을 저장·전송하지 않음(대상 기기로만 전달)을 정책 문서에 명시. |

## 10. 품질 기준 (v1.0 출시 조건)

- 설치부터 첫 전환까지 **5분 이내** (처음 쓰는 사람 3명 기준)
- 전환 단축키 → 대상에 입력 도착 **체감 지연 없음** (측정 목표 < 100ms)
- 1시간 연속 사용 중 **끊김 0회, 키가 눌린 채 남는 현상 0회**
- 호환성 표: 폰 3개 제조사(삼성·픽셀·샤오미) × Android 3개 버전 × 대상 3종(Windows 11, macOS, iPadOS)

## 11. 일정 (초안)

| 단계 | 내용 |
|---|---|
| M0 | 기획 확정 (§12 결정) |
| M1 | 제품 코드 구조로 재편 (PoC 코드 정리, 모듈 분리, Compose 기반) |
| M2 | 호스트 MVP: 온보딩, 대상 관리, 알림·HUD, 자동 시작 |
| M3 | 베타: 지인 테스트, 호환성 표 작성, 배터리 측정 |
| M4 | 배포 (채널은 §12) |

## 12. 결정이 필요한 항목

| # | 질문 | 제안 |
|---|---|---|
| Q1 | v1.0에 **리시버 모드(PC → 폰)** 를 넣을까? Shizuku와 PC 프로그램이 필요하다. | **호스트만 먼저 출시**, 리시버는 Windows 앱과 함께 v1.1 |
| Q2 | 배포 채널: Play 스토어 vs APK 직접 배포 | **초기엔 GitHub Releases**, Play는 접근성 정책 검토 후 별도 결정 |
| Q3 | 앱 이름 `keymanc` 유지? | 제품명 정하면 패키지명도 같이 정리 |
| Q4 | 무료 / 유료 / 오픈소스 | 결정 필요 |
| Q5 | Android에 Rust 코어(FFI)를 넣을까? 호스트(BLE) 경로는 Kotlin만으로 충분하다. | **v1.0은 Kotlin 단독**, Wi-Fi 경로(암호화)를 넣을 때 Rust 코어 도입 |
| Q6 | 태블릿·폴더블·삼성 DeX 대응 수준 | 태블릿 가로 화면 레이아웃은 v1.0에 포함 |
| Q7 | 전환 수단: 단축키만? 알림 버튼·화면 버튼도? | 단축키 + 알림 버튼 |
| Q8 | 클래식 블루투스 HID를 옵션으로 남길까? | 제품 UI에서는 제거, 개발자 메뉴에만 |
